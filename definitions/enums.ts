export enum EmployeeRole {
  Admin = "ADMIN",
  Ops = "OPS",
  Engineer = "ENGINEER",
  Finance = "FINANCE",
}

export enum EmployeeType {
  FullTime = "FULL_TIME",
  PartTime = "PART_TIME",
  Contract = "CONTRACT",
  Intern = "INTERN",
}
